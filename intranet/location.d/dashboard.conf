location /swarm/ {
    rewrite ^/swarm/(.*) /$1  break;
    proxy_pass http://dashboard:8080/$uri$is_args$args;
}

location /stream {
    proxy_pass http://dashboard:8080;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
}
